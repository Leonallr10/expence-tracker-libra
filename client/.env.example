# Production: point to your deployed backend (include /api)
VITE_API_URL=https://expence-tracker-libra-4kg8.vercel.app/api

# Local dev: leave unset — Vite proxy forwards /api to http://localhost:5000
